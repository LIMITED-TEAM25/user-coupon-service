services:
  user-coupon-service:
    container_name: user-coupon-service
    image: chokyuseong/user-coupon-service:latest
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME: ${USERNAME}
        SECRET_KEY: ${SECRET_KEY}
    ports:
      - '19099:19099'
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    mem_limit: 1024mb
    cpus: 1
    networks:
      - limited-team25

networks:
  limited-team25:
    external: true